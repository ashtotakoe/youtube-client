import { CustomColorBarDirective } from './custom-color-bar.directive'

describe('CustomColorBarDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomColorBarDirective()
    expect(directive).toBeTruthy()
  })
})
